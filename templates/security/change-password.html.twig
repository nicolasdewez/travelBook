{% extends 'base.html.twig' %}

{% block body %}
    <div class="mini-body">
        <h2>{{ 'change_password.title'|trans }}</h2>

        <div class="notice">
            {{ 'change_password.notice'|trans }}
        </div>

        {{ form_start(form) }}
            {{ form_widget(form) }}

            <input type="submit" value="{{ 'form.save'|trans }}" class="btn btn-primary pull-right" />
        {{ form_end(form) }}
    </div>
{% endblock %}
