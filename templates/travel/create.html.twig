{% extends 'base.html.twig' %}

{% block body %}
    <h2>{{ 'travels.create.title'|trans }}</h2>

    <div class="mini-body">
        {{ form_start(form) }}

        {{ form_widget(form) }}

        <input type="submit" value="{{ 'form.travels.create.submit'|trans }}" class="btn btn-primary pull-right disabled" disabled="disabled" />
        {{ form_end(form) }}
    </div>
{% endblock %}

{% block javascripts %}
    <script type="text/javascript" src="{{ asset('build/script-travels.js') }}"></script>
    <script type="text/javascript" src="{{ asset('build/script-travels-create.js') }}"></script>
{% endblock %}
