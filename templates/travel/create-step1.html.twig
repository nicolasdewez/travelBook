{% extends 'base.html.twig' %}

{% block body %}
    <h2>{{ 'travels.step1.title'|trans }}</h2>

    <div class="mini-body">
        {{ form_start(form) }}

        {{ form_widget(form) }}

        <input type="submit" value="{{ 'form.travels.step1.submit'|trans }}" class="btn btn-primary pull-right disabled" disabled="disabled" />
        {{ form_end(form) }}
    </div>
{% endblock %}

{% block javascripts %}
    <script type="text/javascript" src="{{ asset('build/script-travels.js') }}"></script>
{% endblock %}
