<nav class="userBar navbar navbar-inverse">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                        <span class="glyphicon glyphicon-user"></span> {{ firstname }} {{ lastname }} ({{ username }}) <span class="caret"></span>
                        <input type="hidden" id="user_lang" value="{{ locale }}" />
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('app_my_account') }}">{{ 'menu.my_account'|trans }}</a></li>
                        <li role="separator" class="divider"></li>
                        <li><a href="{{ path('app_logout') }}">{{ 'menu.logout'|trans }}</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
